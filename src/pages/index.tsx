import { Heading } from '@chakra-ui/react';
import type { NextPage } from 'next';

const Home: NextPage = () => {
  return <Heading color="pink">Hello, Next.js with Chakra UI</Heading>;
};

export default Home;
